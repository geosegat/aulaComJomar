import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const RefundListScreen = () => {
  return (
    <View>
      <Text>RefundList</Text>
    </View>
  );
};

export default RefundListScreen;

const styles = StyleSheet.create({});
